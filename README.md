# TypeScript Server - Grakkit

Clone or Download this repository and extract its contents to `plugins/grakkit`. Then, run `npm install` in both the `plugins/grakkit` and `plugins/grakkit/src` folders.
